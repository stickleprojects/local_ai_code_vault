fastapi>=0.115
uvicorn[standard]>=0.30
# Client minor must track the pinned server (docker-compose.yml:
# qdrant/qdrant:v1.12.4) — an unbounded pin pulled 1.18.x against a
# 1.12 server. Bump both together when upgrading the server image.
qdrant-client>=1.12,<1.13
pydantic>=2.7
pytest>=8
httpx>=0.27

# Indexer (Phase 1.2): tree-sitter core + grammars must stay ABI-matched.
# Core 0.25 reads the 0.23–0.25 grammar ABIs; these ranges have prebuilt
# wheels on CI's py3.12 and local py3.14 alike (no C toolchain needed).
tree-sitter>=0.25,<0.26
tree-sitter-python>=0.23,<0.26
tree-sitter-javascript>=0.23,<0.26
tree-sitter-typescript>=0.23,<0.26
tree-sitter-c-sharp>=0.23,<0.26
