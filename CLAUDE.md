# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is currently a stub. It contains only `README.md` and has had a single "Initial commit". There is no source code, build tooling, dependency manifest, test suite, or CI configuration yet.

The project name (`local_ai_code_vault`) suggests an intended focus on local AI / code storage, but no implementation exists to confirm direction.

## When the codebase grows

This file should be updated once real code lands. At that point, document:

- Build, lint, and test commands (including how to run a single test)
- The high-level architecture that spans multiple files and isn't obvious from a quick look
- Any project-specific conventions established in code or contributor docs

Until then, there is no architecture or workflow to describe — inspect the working tree directly to confirm current state before relying on this document.
