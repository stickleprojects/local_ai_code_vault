# local_ai_code_vault

This project attempts to host a vault of your code to speed up claude queries - instead of claude reacing the files it reads the db, commit hooks keep the db uptodate.
designed to be a docker-contained vault of your code

# usage

1. run the vaults via docker compose
2. open your repo workspace
3. install the skills
4. ask claude to register
